/*12.有一阶跃函数：
y={█(-1     (x<0)@0        (x=0)@1       (x>0))┤
编写程序，输入一个x值，要求输出y相应的值。
*/
#include<stdio.h>
int main()
{
	double x, y;
	printf("请输入一个x:");
	scanf("%lf", &x);
	if (x < 0)
		printf("y=-1\n");
	else
	{
		if (x == 0)
			printf("y=0\n");
		else
			printf("y=1\n");
	}
	return 0;
}
