//18．编写程序，从键盘上输入一个年份，判断其是否为闰年。（说明：凡能被4整除但不能被100整除，或能被400整除的年份为闰年。）
#include<stdio.h>
int main()
{
	int y;
	printf("请输入一个年份:");
	scanf("%d", &y);
	if (y % 4 == 0 && y % 100 != 0 || y % 400 == 0)
		printf("%d为闰年\n", y);
	else
		printf("%d不是闰年\n", y);
	return 0;
}
