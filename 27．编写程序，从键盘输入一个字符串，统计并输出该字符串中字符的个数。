//27．编写程序，从键盘输入一个字符串，统计并输出该字符串中字符的个数。
#include<stdio.h>
int main()
{
	char ch[50];
	int i, count = 0;
	printf("请输入一个字符串:");
	scanf("%s", &ch);
	for (i = 0; ch[i] != '\0'; i++)
		count++;
	printf("字符个数是%d\n", count);
	return 0;
}
