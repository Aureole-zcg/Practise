//24．编写程序，任意输入10个整数，找出其中最大的数并输出。
#include<stdio.h>
int main()
{
	int a[10], i, max;
	printf("请输入10个整数:");
	for (i = 0; i < 10; i++)
		scanf("%d", &a[i]);
	max = a[0];
	for (i = 1; i < 10; i++)
	{
		if (a[i] > max)
			max = a[i];
	}
	printf("最大的数:%d\n", max);
	return 0;
}
