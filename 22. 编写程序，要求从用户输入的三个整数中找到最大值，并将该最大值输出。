//22. 编写程序，要求从用户输入的三个整数中找到最大值，并将该最大值输出。
#include<stdio.h>
int main()
{
	int x, y, z, max;
	printf("请输入三个整数:");
	scanf("%d%d%d", &x, &y, &z);
	max = x;
	if (y > max)
		max = y;
	if (z > max)
		max = z;
	printf("最大值是%d\n", max);
	return 0;
}
